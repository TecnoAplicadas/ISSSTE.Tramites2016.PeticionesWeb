@model IEnumerable<ISSSTE.TramitesDigitales2016.Modelos.Contextos.pa_PeticionesWeb_Bandeja_Peticiones_Obtiene_Operador_Por_UPS_Result>
@using PagedList.Mvc;
@{
    ViewBag.Title = "AsignarOperador";
    Layout = "~/Views/Shared/_LayoutPeticiones.cshtml";
}



<div class="row">
   <div class="col-md-12">
      <h2>@Html.Label("Asignar operador")</h2>
      <hr class="Negro1" />
   </div>
</div>


@using (Html.BeginForm("AsignarOperador", "BandejaDepeticiones", FormMethod.Post, new { enctype = "multipart/form-data" }))
{


@*<div id="loadingDisplay" class="progressBarLoad">
    <div class="progressBarLoad-content">

        <div class="container-fluid">*@
            @*<div class="panel panel-info">*@
                @*<div class="panel-heading ">Asignar Operador</div>*@
                <div class="panel-body">
                    <div class="col-lg-12">

                        <div class="row form-group">
                            <label for="SelectedUserRoleId">Operadores:</label>
                            @foreach (var Operadores in Model)
                            {
                                <br />
                                @*@Html.RadioButton(Operadores.NombreOperador, true, new { @checked = "checked" })
                                @Operadores.NombreOperador*@
                                @Html.RadioButtonFor(x => x.First().NombreOperador,
                                                      new { Operadores.IdUsuario },
                                                      new { @checked = "checked" })
                                @Operadores.NombreOperador
                                @Html.Hidden("IdUsuario")
                            }
                            
                            @*@Html.Hidden("OperaodrSelecciondado", Model.)*@

                           

                        </div>

                        <div class="row form-group">

                            <button class="btn  btn-sm btn-primary" id="enviarSol" name="enviarSol" type="submit">
                                <span>Asignar</span>
                                <span class="glyphicon glyphicon-user"></span>
                            </button>

                            @*<button onclick="location.href='@Url.Action("BandejaIndex", "BandejaDepeticiones")';return false;"
                                    class="btn btn-info btn-sm">
                                Aceptar
                            </button>*@
                            <button onclick="location.href='@Url.Action("BandejaIndex", "BandejaDepeticiones")';return false;"
                                    class="btn btn-danger btn-sm">
                                <span>Cancelar</span>
                                <span class="glyphicon glyphicon-remove"></span>
                            </button>
                        </div>
                    </div>
                </div>
            @*</div>*@
        @*</div>

    </div>
</div>*@

}
@section Scripts{
   <script type="text/javascript">
      $(document).ready(function () {
            $("#liBandeja").addClass('active');
            $("#liBitacora").removeClass('active');
            $("#liDias").removeClass('active');
            $("#liReportes").removeClass('active');
            $("#liTablero").removeClass('active');
      });
   </script>
}